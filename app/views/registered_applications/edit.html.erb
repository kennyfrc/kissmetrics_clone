<h1>Edit Application</h1>
 
 <div class="row">
   <div class="col-md-6">
     <%= form_for @registered_application do |f| %>
       <div class="form-group">
         <%= f.label :name %>
         <%= f.text_field :name, class: 'form-control', placeholder: "Enter application" %>
       </div>
       <div class="form-group">
         <%= f.label :url %>
         <%= f.text_area :url, class: 'form-control', placeholder: "Enter url description" %>
       </div>
       <%= f.submit "Save", class: 'btn btn-success' %>
       <%= link_to "Back to Index", registered_applications_path, class: "btn btn-warning" %>
     <% end %>
   </div>
   <div class="col-md-4">
     <h3>Guidelines for editing:</h3>
     <ul>
       <li>Make sure that it's in the approved applications list (List is TBC).</li>
     </ul>
   </div>
 </div>